# Development Dockerfile for airis-mindbase workspace
FROM node:22-alpine

# Install pnpm
RUN corepack enable && corepack prepare pnpm@10.22.0 --activate

# Set working directory
WORKDIR /app

# Keep container running
CMD ["sleep", "infinity"]
